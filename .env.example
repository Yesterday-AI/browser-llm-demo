# No runtime env vars required.
# All configuration is compile-time:
#   - src/lib/model-catalog.ts  — supported models
#   - src/lib/project.ts        — repo URL, project identity
#   - src/lib/i18n/*.ts         — UI strings, taglines
